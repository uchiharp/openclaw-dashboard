# 🦞 OpenClaw Dashboard — 功能介绍

## 解决什么痛点？

OpenClaw 的 Agent 系统强大，但**缺少可视化**。所有的 Agent 状态、会话记录、Token 消耗都散落在命令行和 JSON 文件里。想了解"谁在跑、用了多少 Token、出了什么错"，只能手动查文件、敲命令。

Dashboard 解决的核心痛点：

| 痛点 | 之前 | 现在 |
|------|------|------|
| 不知道 Agent 状态 | `openclaw agent list` 命令行查看 | 左侧列表一目了然，30秒自动刷新 |
| 会话太多占资源 | 手动删 JSONL 文件 | 一键归档（AI 蒸馏摘要）或重置 |
| Token 超限崩了才知道 | 无预警 | 使用率进度条，超 70% 红色警示 |
| 出错了不知道 | 看终端日志 | 错误日志汇总，按时间排序 |
| 想看某个会话聊了啥 | 翻 JSONL 文件（巨长） | 点击展开，结构化显示对话 |
| 想终止异常会话 | 找 session ID，敲命令 | 点一下按钮 |

---

## 功能详解

### 📊 系统总览

打开 Dashboard 第一眼看到的全局状态：

- **Agent 数量** — 有多少个 Agent 在工作
- **运行中 / 异常** — 实时统计
- **磁盘占用** — 所有 Agent 的 session 文件总大小
- **Token 使用率排行** — 哪个 Agent 的上下文快满了
- **磁盘占用排行** — 哪个 Agent 占用最多空间

> 💡 适合每天扫一眼，快速了解系统健康状况。

### 🤖 Agent 管理

左侧边栏列出所有 Agent，按**最近活跃时间**排序：

- 最上面是最活跃的 Agent，idle 最久的沉到底部
- 每个卡片显示：会话数、运行状态、最大使用率
- 点击进入详情页，查看会话和错误日志

### 💬 会话查看

Agent 详情页的会话列表，**点击可展开对话内容**：

- 用户消息（蓝色气泡，靠右）
- AI 回复（灰色气泡，靠左）
- 工具调用（蓝色标签，显示工具名和参数）
- 工具结果（绿色字体）
- 对话太长时支持**分页加载**

状态指示：
- 🟡 运行中（黄色呼吸灯）
- 🟢 已完成
- 🔴 失败
- 🟠 超时/未知

### 📦 AI 蒸馏归档

这是最核心的功能——**智能清理过期会话**。

**传统方式：**
```
会话满了 → 手动删除 → 对话内容丢失 → 不知道之前聊了啥
```

**Dashboard 归档：**
```
会话满了 → 点「归档」→ 自动备份 → AI 生成 300 字摘要 → 清理文件
→ 摘要保存在本地，需要时还能查看
```

流程：
1. 备份所有 JSONL 对话文件到安全目录
2. 逐个会话调用 AI，蒸馏为精简摘要
3. 摘要保存为 `.summary.md` 文件
4. 从 sessions.json 删除已归档会话
5. 删除原始 JSONL 文件释放空间

> 💡 归档是异步执行的，不会阻塞操作。归档过程中可以继续使用 Dashboard。

### ⚡ 实时监控

基于 SSE（Server-Sent Events）的实时推送：

- 会话被终止 → 自动刷新列表
- Agent 被重置 → 自动返回总览
- 归档完成 → 自动刷新 + Toast 通知

不需要手动刷新页面，30秒也有兜底轮询。

### 🔧 错误诊断

Agent 详情页底部有错误日志面板：

- 优先从 Gateway 系统日志中提取
- 不够时扫描最近 5 个 JSONL 会话文件
- 按时间排序，最新的在最上面
- 显示错误来源（系统日志 / 会话文件）

### ⏰ 定时任务监控

左侧边栏底部显示 Cron 任务状态：

- ✅ 上次成功 / ❌ 上次失败
- 任务名 + 调度频率

---

## 适用场景

### 日常使用

每天打开 Dashboard 扫一眼，了解 Agent 系统健康状态，比翻命令行方便 10 倍。

### 多 Agent 管理

有 10+ 个 Agent 同时运行时，Dashboard 的排序和总览让你快速找到需要关注的那个。

### 资源管理

Token 上下文快满时红色警示，磁盘占用过大一目了然。及时归档释放资源，避免浪费。

### 问题排查

错误日志汇总 + 会话对话展开，定位问题比翻日志快很多。

---

## 技术亮点

- **零构建** — 纯 HTML + Alpine.js，不需要 Webpack/Vite
- **单文件前端** — 整个 UI 就一个 `index.html`，方便修改
- **纯 Node.js 后端** — 不依赖数据库，直接读取 OpenClaw 文件
- **AI 蒸馏** — 归档时调用 `openclaw agent` CLI，复用现有 AI 配置
- **安全防护** — 路径遍历校验、参数上限、Token 认证
- **实时推送** — SSE 长连接，30秒兜底轮询
